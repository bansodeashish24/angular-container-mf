import { environment } from '../../environments/environment';

// Use these endpoints in services to call APIs
export class ApiEndpoints {
  public static readonly METHOD_API_ENDPOINT_NAME = `${environment.baseUrlMicroService1}/endpoint`; // TODO. This is an Example.
}
